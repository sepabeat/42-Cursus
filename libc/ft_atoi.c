//header 42

#include <stdio.h>
#include <stdlib.h>

int ft_atoi(const char *nptr)
{
    
    return (0);
}

int main (void)
{
    const char  str[] = "123"; 
    printf("%d", atoi(str));
    printf("%d", ft_atoi(str));
    return (0);
}